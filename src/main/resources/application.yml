spring:
  application:
    name: InventoryManagementSystem

#  datasource:
#    url: jdbc:postgresql://localhost:5432/studytracker
#    username: postgres
#    password: root
#    driver-class-name: org.postgresql.Driver
  datasource:
   url: jdbc:postgresql://ep-restless-mountain-a8dje1pl-pooler.eastus2.azure.neon.tech:5432/neondb?sslmode=require
   username: neondb_owner
   password: npg_hiYq4uOWeZo2
   driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 2GB
      max-request-size: 2GB

server:
  port: 5050

secreteJwtString: phegondev123456789phegondev123456789

application:
  file:
    uploads:
      photos-output-path: ./src/main/resources/static/images
      photos-access-path: /images

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE